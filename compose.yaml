services:
  backend:
    build:
      context: ./app/backend/
      dockerfile: ./Dockerfile
    restart: always
    ports:
      - 127.0.0.1:8000:8000
    expose:
      - 8000

  frontend:
    build:
      context: ./app/frontend/
      dockerfile: ./Dockerfile
    restart: always
    ports:
      - 86.50.253.176:443:443
    expose:
      - 443

